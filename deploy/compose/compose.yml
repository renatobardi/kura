name: kura-prod

services:
  relay:
    image: ${KURA_IMAGE:-ghcr.io/block/kura:main}
    env_file:
      - .env
    environment:
      KURA_BIND_ADDR: 0.0.0.0:3000
      KURA_HEALTH_PORT: "8080"
      KURA_METRICS_PORT: "9102"
      DATABASE_URL: postgres://${POSTGRES_USER:-kura}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-kura}
      REDIS_URL: redis://:${REDIS_PASSWORD:?set REDIS_PASSWORD}@redis:6379
      KURA_S3_ENDPOINT: http://minio:9000
      # Docker DNS resolves `minio`, not arbitrary `<bucket>.minio` hosts.
      KURA_S3_ADDRESSING_STYLE: path
      KURA_S3_ACCESS_KEY: ${KURA_S3_ACCESS_KEY:?set KURA_S3_ACCESS_KEY}
      KURA_S3_SECRET_KEY: ${KURA_S3_SECRET_KEY:?set KURA_S3_SECRET_KEY}
      KURA_S3_BUCKET: ${KURA_S3_BUCKET:-kura-media}
      KURA_GIT_REPO_PATH: /data/git
      KURA_AUTO_MIGRATE: ${KURA_AUTO_MIGRATE:-false}
      KURA_GIT_CONFORMANCE_PROBE: ${KURA_GIT_CONFORMANCE_PROBE:-true}
    ports:
      - "${KURA_HTTP_PORT:-3000}:3000"
    volumes:
      - kura-git-data:/data/git
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    # Probe /_readiness over /dev/tcp because the runtime image has bash but no curl/wget/socat.
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -ec 'exec 3<>/dev/tcp/127.0.0.1/8080; printf \"GET /_readiness HTTP/1.1\\r\\nHost: 127.0.0.1\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"200 OK\" <&3'",
        ]
      interval: 10s
      timeout: 3s
      retries: 12
      start_period: 30s
    restart: unless-stopped
    networks:
      - kura-net

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-kura}
      POSTGRES_USER: ${POSTGRES_USER:-kura}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - kura-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped
    networks:
      - kura-net

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD:?set REDIS_PASSWORD}"]
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD}
    volumes:
      - kura-redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep -q PONG"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped
    networks:
      - kura-net

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${KURA_S3_ACCESS_KEY:?set KURA_S3_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${KURA_S3_SECRET_KEY:?set KURA_S3_SECRET_KEY}
    volumes:
      - kura-minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped
    networks:
      - kura-net

  minio-init:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    depends_on:
      minio:
        condition: service_healthy
    environment:
      KURA_S3_ACCESS_KEY: ${KURA_S3_ACCESS_KEY:?set KURA_S3_ACCESS_KEY}
      KURA_S3_SECRET_KEY: ${KURA_S3_SECRET_KEY:?set KURA_S3_SECRET_KEY}
      KURA_S3_BUCKET: ${KURA_S3_BUCKET:-kura-media}
    entrypoint: >
      /bin/sh -euc '
        mc alias set local http://minio:9000 "$${KURA_S3_ACCESS_KEY}" "$${KURA_S3_SECRET_KEY}"
        mc mb --ignore-existing "local/$${KURA_S3_BUCKET}"
        mc anonymous set none "local/$${KURA_S3_BUCKET}"
      '
    restart: "no"
    networks:
      - kura-net

volumes:
  kura-postgres-data:
    labels:
      com.kura.volume: postgres
  kura-redis-data:
    labels:
      com.kura.volume: redis
  kura-minio-data:
    labels:
      com.kura.volume: minio
  kura-git-data:
    labels:
      com.kura.volume: git

networks:
  kura-net:
    driver: bridge
    labels:
      com.kura.network: production
