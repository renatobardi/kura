[package]
name = "kura-admin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Operator CLI for Kura relay administration"

[[bin]]
name = "kura-admin"
path = "src/main.rs"

[dependencies]
kura-db = { workspace = true }
kura-deletion = { workspace = true }
kura-core = { workspace = true }
kura-auth = { workspace = true }
kura-pubsub = { workspace = true }
kura-search = { workspace = true }
kura-audit = { workspace = true }
kura-workflow = { workspace = true }
kura-media = { workspace = true }
nostr = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
hex = { workspace = true }
deadpool-redis = { workspace = true }
# Redis TLS (rediss://, e.g. ElastiCache) uses rustls, which needs a process
# CryptoProvider installed at startup. The workspace redis TLS feature compiles
# both aws-lc-rs and ring in transitively, so rustls can't auto-select one — we
# install ring explicitly in main(). Mirrors kura-relay's setup.
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
tracing = { workspace = true }
sqlx = { workspace = true }
url = { workspace = true }
uuid = { workspace = true }
clap = { version = "4", features = ["derive"] }
