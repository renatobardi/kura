name: kura

services:
  postgres:
    image: postgres:17-alpine
    container_name: kura-postgres
    environment:
      POSTGRES_USER: kura
      POSTGRES_PASSWORD: kura_dev
      POSTGRES_DB: kura
      PGDATA: /var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - kura-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kura"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.kura.service: "postgres"
      com.kura.env: "dev"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: kura-redis
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - kura-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.kura.service: "redis"
      com.kura.env: "dev"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: kura-adminer
    ports:
      - "127.0.0.1:8082:8080"
    networks:
      - kura-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.kura.service: "adminer"
      com.kura.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: kura-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "127.0.0.1:8180:8080"
    networks:
      - kura-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.kura.service: "keycloak"
      com.kura.env: "dev"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: kura-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: kura_dev
      MINIO_ROOT_PASSWORD: kura_dev_secret
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - kura-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.kura.service: "minio"
      com.kura.env: "dev"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: kura-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - kura-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 kura_dev kura_dev_secret &&
        mc mb --ignore-existing local/kura-media &&
        mc anonymous set none local/kura-media
      "
    labels:
      com.kura.service: "minio-init"
      com.kura.env: "dev"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: kura-prometheus
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - kura-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.kura.service: "prometheus"
      com.kura.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: kura-postgres-data
    labels:
      com.kura.volume: "postgres"
  minio-data:
    name: kura-minio-data
    labels:
      com.kura.volume: "minio"
  prometheus-data:
    name: kura-prometheus-data
    labels:
      com.kura.volume: "prometheus"

networks:
  kura-net:
    name: kura-net
    driver: bridge
    labels:
      com.kura.network: "dev"
